name: Create APKs Builds

on:
  push:
    branches: ["expo-practice"]
jobs:
  build_android:
    type: build
    params:
      platform: android
      profile: standalone
  fingerprint:
    name: Tradly Fingerprint
    type: fingerprint
    environment: preview
